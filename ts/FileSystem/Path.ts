import { FSharpList } from "../fable_modules/fable-library-ts/List.js";

export function combine(path1: string, path2: string): string {
    throw new Error();
}

export function combineMany(paths: string[]): string {
    throw new Error();
}

export function split(separator: string, path: string): FSharpList<string> {
    throw new Error();
}

